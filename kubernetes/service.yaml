apiVersion: v1
kind: Service
metadata:
  name: rails-microcommerce
  namespace: rails-microcommerce
  labels:
    app: rails-microcommerce
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
  selector:
    app: rails-microcommerce
